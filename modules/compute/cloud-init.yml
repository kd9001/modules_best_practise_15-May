#cloud-config
package_update: true
packages:
  - nginx

runcmd:
  - systemctl start nginx
  - systemctl enable nginx
  - echo "<h1>Hello from 3-Tier Architecture on Azure</h1>" > /var/www/html/index.html